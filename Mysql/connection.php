
    <?php

/*$host = 'localhost'; // Replace with your MySQL server host if necessary
$username = 'Admin'; // Replace with your MySQL username
$password = '1250981453'; // Replace with your MySQL password
$database = 'phpDatabase'; // Replace with your database name

// Create a new MySQLi object
$connection = new mysqli($host, $username, $password, $database);

// Check if the connection was successful
if ($connection->connect_error) {
  die('Connection failed: ' . $connection->connect_error);
}

// Connection successful, you can now execute queries using $connection
?>
<?php
// Assuming you have already established the $connection object

// Example query to select data from a table
$query = "SELECT * FROM your_table";
$result = $connection->query($query);

// Process the query result
if ($result->num_rows > 0) {
  while ($row = $result->fetch_assoc()) {
    // Process each row of data
  }
} else {
  // No results found
}

// Remember to close the connection when you're done
$connection->close();
?>*/

